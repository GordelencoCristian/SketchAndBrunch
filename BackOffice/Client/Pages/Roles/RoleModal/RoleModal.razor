<MudDialog>
    <DialogContent>
        <EditForm EditContext="@_roleEditContext" OnSubmit="() => ValidateContext()">
            <DataAnnotationsValidator/>
            <MudItem >
                <MudCard Elevation="0">
                    <MudCardContent>
                        <MudTextField Label="Role name" @bind-Value="Role.Name" For="@(() => Role.Name)" />


                    @*  <MudSelect @bind-Value="Model.Role" Label="Select Role" Validation="@_userProfileEditContext" Placeholder="Please Select Role" Class="mt-6">
                            @foreach (var item in RoleList)
                            {
                                <MudSelectItem Value="@item">@item.Name</MudSelectItem>
                            }
                        </MudSelect> *@
                        
                        <MudSelect T="string" Label="US States" Error="_showPermitionsError" ErrorText="Should be selected Permisions" MultiSelection="true" Class="mt-6" @bind-SelectedValues="Role.Permistions">
                            @foreach (var state in Permistions)
                            {
                                <MudSelectItem T="string" Value="@state">@state</MudSelectItem>
                            }
                        </MudSelect>

                    </MudCardContent>
                </MudCard>
            </MudItem>
        </EditForm>

    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="ValidateContext">Ok</MudButton>
    </DialogActions>
</MudDialog>
